spring:
  application:
    name: springCars

  datasource:
    url: jdbc:postgresql://localhost:5432/cars
    username: user
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

custom:
  maxCar: 10
  table:
    sorting:
      disabled-fields:
        - maxSpeed

loan:
  minSalary: 40000
  minCarPrice: 1000000

url:
  "https://66055cd12ca9478ea1801f2e.mockapi.io/api/users/income"
